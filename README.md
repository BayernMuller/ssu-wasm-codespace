# ssu-wasm-codespace

## Codespace 실행

1. `Code` 버튼을 누르고 `Codespaces` 탭에서 `Create codespace on main`을 선택한다.
2. 편집기가 열릴 때까지 기다린다. 첫 실행은 몇 분 걸릴 수 있다.
3. 터미널에서 도구가 준비되었는지 확인한다.

```bash
clang++ --version
em++ --version
```

두 명령이 버전을 출력하면 준비가 끝난 것이다.
